{{ form_start(form) }}
	<div class="row">
		<div class="col col-md-4">
			{{ form_row(form.identite.nom) }}
		</div>
		<div class="col col-md-4">
			{{ form_row(form.identite.postnom) }}
		</div>
		<div class="col col-md-4">
			{{ form_row(form.identite.prenom) }}
		</div>
		<div class="col col-md-4">
			{{ form_row(form.adresse.telephone) }}
		</div>
		<div class="col col-md-4">
			{{ form_row(form.adresse.ville) }}
		</div>
		<div class="col col-md-4">
			{{ form_row(form.adresse.pays) }}
		</div>
		<div class="col col-md-8">
			{{ form_row(form.adresse.adresse) }}
		</div>
		<div class="col col-md-4">
			{{ form_row(form.zoneLivraisonPreferentielle) }}
		</div>
	</div>
	<div class="row">
		<div class="col-12">
			&nbsp;
			<a class="btn bnt-link" data-toggle="modal" data-target="#myModal">Lire les conditions d'utilisation</a>
			<br/><br/>
		</div>
	</div>
	<div class="row">
		<div class="col col-md-6">
			{{ form_row(form.agreeTerms) }}
		</div>
		<div class="col col-md-6">
			<button type="submit" class="btn btn-default btn-primary pull-right">
				<span class="fa fa-user">
					Enregistrer
				</button>
			</div>
		</div>
		{{ form_end(form) }}

	</div>
